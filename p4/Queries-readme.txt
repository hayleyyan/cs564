Query1
	have enough features

Query2
	have enough features

Query3
	have enough features

Query4
	have enough features

Query5
	have enough features

Query6
	have enough features

Query7
	have enough features
